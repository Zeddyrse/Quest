import { SaiyensDirective } from './saiyens.directive';

describe('SaiyensDirective', () => {
  it('should create an instance', () => {
    const directive = new SaiyensDirective();
    expect(directive).toBeTruthy();
  });
});
