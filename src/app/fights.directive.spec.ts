import { FightsDirective } from './fights.directive';

describe('FightsDirective', () => {
  it('should create an instance', () => {
    const directive = new FightsDirective();
    expect(directive).toBeTruthy();
  });
});
